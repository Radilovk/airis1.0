# Стъпка по Стъпка: Как да Отстраним Проблема със Зареждането

## 🚨 Приложението не стартира? Ето как да го поправим:

### Стъпка 1: Проверете дали вижте нещо на екрана

**Ако виждате само празна бяла страница:**
1. Натиснете `F12` на клавиатурата (отваря се Developer Console)
2. Кликнете на раздел "Console"
3. Потърсете червени съобщения за грешки
4. Копирайте текста на грешката

**Ако виждате съобщение за грешка на български:**
- Приложението е заредено! Грешката е в изпълнението
- Използвайте QuickDebugPanel (жълт бутон долу вдясно)

**Ако не виждате нищо:**
- Проблемът е критичен, вероятно при зареждането
- Продължете със Стъпка 2

### Стъпка 2: Използвайте QuickDebugPanel

След като приложението се зареди (дори и частично), долу вдясно ще видите **жълт кръгъл бутон с икона на бъг** 🐛.

**Кликнете го!** Ще се отвори панел с бърза диагностика:

- 🟢 **Зелени точки** = Всичко е наред
- 🟡 **Жълти точки** = Има предупреждение
- 🔴 **Червени точки** = Критична грешка

**Най-често срещани проблеми:**

| Грешка | Какво означава | Как да се поправи |
|--------|---------------|------------------|
| **Spark API червен** | Основният API не работи | Презаредете страницата (Ctrl+R) |
| **KV Storage червен** | Проблем със съхранението | Кликнете "Изтрий Всички Данни" в Диагностика |
| **User API червен** | Не може да се определи потребителя | Излезте и влезте отново в GitHub |
| **Browser APIs червен** | Браузърът не поддържа функциите | Използвайте по-нов браузър (Chrome, Firefox, Edge) |

### Стъпка 3: Проверете Конзолата на Браузъра

Ако QuickDebugPanel не се появява, проблемът е по-сериозен.

**Как да отворите конзолата:**
- Windows/Linux: `F12` или `Ctrl+Shift+J`
- Mac: `Cmd+Option+J`

**Какво да търсите:**

```
❌ Червени съобщения започващи с:
   - "Error loading module"
   - "Failed to fetch"
   - "Cannot read property"
   - "is not defined"
```

**Най-често срещани грешки в конзолата:**

#### Грешка 1: "Cannot find module" или "Failed to resolve"
```
Error: Cannot find module '@/components/screens/XYZScreen'
```

**Проблем:** Липсва файл или грешен import

**Решение:**
1. Проверете дали файлът съществува на посоченото място
2. Проверете правописа в import-а
3. Презаредете страницата

#### Грешка 2: "window.spark is not defined"
```
ReferenceError: spark is not defined
```

**Проблем:** Spark API не е зареден

**Решение:**
1. Презаредете страницата (Ctrl+R)
2. Изчистете кеша: Ctrl+Shift+Del → "Cached images and files"
3. Ако не помага, затворете и отворете браузъра

#### Грешка 3: "Cannot read property of undefined"
```
TypeError: Cannot read property 'zones' of undefined
```

**Проблем:** Липсват данни

**Решение:**
1. Отворете QuickDebugPanel
2. Проверете дали KV Storage е зелен
3. Ако не е, изтрийте данните и започнете отначало

#### Грешка 4: "Unexpected token" или "Syntax error"
```
SyntaxError: Unexpected token '<'
```

**Проблем:** Грешка в кода или проблем със зареждането

**Решение:**
1. Изчистете кеша на браузъра
2. Презаредете страницата с `Ctrl+Shift+R` (hard reload)
3. Проверете дали има грешки в кода

### Стъпка 4: Използвайте Пълния Диагностичен Екран

Ако приложението се зарежда до главния екран:

1. Уверете се, че сте собственик на приложението
2. На главния екран потърсете бутон **"Диагностика"** (жълт цвят)
3. Кликнете го
4. Ще видите пълен списък с проверки

**В диагностичния екран можете да:**
- ✅ Видите детайлна информация за всяка проверка
- 📥 Експортирате резултатите като JSON
- 🗑️ Изтриете всички данни (ако е необходимо)

### Стъпка 5: Изтриване на Всички Данни (Крайна мярка)

**⚠️ ВНИМАНИЕ: Това ще изтрие:**
- Всички запазени анкети
- Всички качени снимки
- Цялата история на анализи
- Всички настройки

**Как да го направите:**

**Вариант А: Чрез Диагностичния Екран**
1. Отидете в Диагностика
2. Scroll до "Опасна Зона"
3. Кликнете "Изтрий Всички Данни"
4. Потвърдете
5. Страницата ще се презареди автоматично

**Вариант Б: Чрез QuickDebugPanel**
1. Кликнете жълтия бутон долу вдясно
2. (Ако има опция за изтриване, използвайте я)

**Вариант В: Ръчно чрез Конзолата**
1. Натиснете F12
2. Отидете в раздел "Console"
3. Копирайте и изпълнете:

```javascript
// Изтрива всички KV данни
(async () => {
  const keys = await spark.kv.keys()
  for (const key of keys) {
    await spark.kv.delete(key)
  }
  localStorage.clear()
  console.log('Всички данни са изтрити')
  setTimeout(() => location.reload(), 1000)
})()
```

### Стъпка 6: Ако Нищо Не Помага

**Проверка на Browser API-тата:**

В конзолата (F12 → Console) изпълнете:

```javascript
// Проверка на основни APIs
console.log({
  spark: typeof window.spark !== 'undefined',
  sparkKV: typeof window.spark?.kv !== 'undefined',
  sparkLLM: typeof window.spark?.llm !== 'undefined',
  sparkUser: typeof window.spark?.user !== 'undefined',
  localStorage: typeof localStorage !== 'undefined',
  fetch: typeof fetch !== 'undefined',
  Promise: typeof Promise !== 'undefined'
})
```

**Резултат трябва да е:**
```javascript
{
  spark: true,
  sparkKV: true,
  sparkLLM: true,
  sparkUser: true,
  localStorage: true,
  fetch: true,
  Promise: true
}
```

**Ако някой от тези е `false`:**
- Презаредете страницата
- Изчистете кеша
- Опитайте в друг браузър
- Проверете дали използвате последната версия на браузъра

---

## 🔍 Специфични Проблеми

### Проблем: "Приложението се зарежда, но не мога да стартирам анализ"

**Възможни причини:**
1. Липсват данни от въпросника
2. Липсват снимки на ириси
3. Проблем с LLM API

**Решение:**
1. Кликнете "Започни Анализ" и попълнете въпросника отначало
2. Качете нови снимки
3. Проверете Admin настройките (ако сте собственик)

### Проблем: "Не мога да видя историята"

**Възможни причини:**
1. Няма запазени анализи
2. Проблем с KV Storage

**Решение:**
1. Проверете QuickDebugPanel → KV Storage трябва да е зелен
2. Ако е червен, изтрийте данните
3. Направете нов анализ

### Проблем: "Докладът не се показва правилно"

**Възможни причини:**
1. Липсват данни в доклада
2. Грешка в компонента за визуализация

**Решение:**
1. Проверете конзолата за грешки (F12)
2. Направете нов анализ
3. Ако проблемът продължава, изтрийте данните

### Проблем: "QuickDebugPanel не се появява"

**Възможни причини:**
1. Приложението не се е заредило изобщо
2. JavaScript грешка преди рендъра

**Решение:**
1. Проверете конзолата (F12)
2. Търсете червени грешки
3. Презаредете страницата с Ctrl+Shift+R

---

## 📋 Checklist за Отстраняване на Проблеми

Преди да търсите помощ, уверете се че сте направили следното:

- [ ] Презаредил/а съм страницата (Ctrl+R)
- [ ] Изчистил/а съм кеша (Ctrl+Shift+Del)
- [ ] Проверил/а съм конзолата за грешки (F12)
- [ ] Използвал/а съм QuickDebugPanel (жълт бутон)
- [ ] Проверил/а съм пълната диагностика (Диагностика екран)
- [ ] Опитал/а съм в друг браузър
- [ ] Експортирал/а съм диагностичните данни
- [ ] (Ако е необходимо) Изтрил/а съм всички данни

---

## 🆘 Как да Съобщите за Проблем

Ако нищо не помага, съберете следната информация:

1. **Експорт от диагностиката** (JSON файл)
2. **Screenshot на грешката** (ако има визуална грешка)
3. **Текст от конзолата** (F12 → Console, copy всички червени съобщения)
4. **Стъпките които доведоха до проблема**
5. **Браузър и операционна система**

Пример:
```
Браузър: Chrome 120.0
ОС: Windows 11
Проблем: Приложението не се зарежда
Грешка в конзолата: "Cannot find module '@/components/screens/XYZ'"
Експорт: [прикачете JSON файла]
Стъпки: 
1. Отворих приложението
2. Виждам бяла страница
3. Има грешка в конзолата
```

---

## ✅ Превантивни Мерки

За да избегнете проблеми в бъдеще:

1. **Редовно проверявайте диагностиката** (веднъж седмично)
2. **Не изтривайте данни без да експортирате докладите**
3. **Използвайте последна версия на браузъра**
4. **Избягвайте многократно натискане на бутони** (изчакайте отговор)
5. **При съмнение за проблем, веднага проверете QuickDebugPanel**

---

## 🎯 Кратки Препратки

| Какво | Къде | Как |
|-------|------|-----|
| QuickDebugPanel | Долу вдясно | Жълт бутон с бъг |
| Диагностика | Главен екран | Жълт бутон "Диагностика" |
| Конзола | Браузър | F12 → Console |
| Изтриване на данни | Диагностика | "Опасна Зона" секция |
| Експорт | Диагностика | Бутон "Експорт" |

---

**Съвет:** Запазете този документ! Той съдържа всичко необходимо за бързо решаване на проблеми. 🔖
