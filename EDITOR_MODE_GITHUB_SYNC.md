# Editor Mode - Подобрена Система с GitHub Синхронизация

## 🎯 Обща Информация

Новата версия на Editor Mode е значително подобрена и разширена със следните ключови функции:

1. **Редактиране на Исторически Репорти** - Можете да редактирате стари репорти от историята
2. **GitHub Синхронизация** - Промените от Editor Mode се прилагат директно в кода на repository
3. **Генериране на Код** - Автоматично се генерира React/TypeScript код от промените
4. **Pull Request Създаване** - Директно създаване на PR от интерфейса

## 📋 Как Работи

### 1. Редактиране на Текущи Репорти

Когато сте в Report Screen:
1. Отидете в Admin Panel → Editor Mode
2. Включете Editor Mode превключвателя
3. Всички модули и елементи стават редактируеми
4. Правите промени (скриване, преместване, коментари)
5. Промените се запазват автоматично

### 2. Редактиране на Исторически Репорти

Когато искате да редактирате стар репорт:
1. Отидете в History Screen
2. Намерете репорта, който искате да редактирате
3. Натиснете бутона "Редактирай" (лилав бутон с молив)
4. Отваря се специален редактор за този репорт
5. Правите промени и ги запазвате
6. Всеки репорт има своя собствена editor конфигурация

**Важно:** Промените в историческите репорти не влияят на текущите репорти. Всеки репорт има своя независима конфигурация.

### 3. GitHub Синхронизация - Прилагане на Промените в Кода

Това е най-важната нова функция! Промените от Editor Mode вече могат да се приложат директно в repository кода.

#### Стъпка 1: Конфигуриране на GitHub Token

1. Отидете в GitHub → Settings → Developer Settings → Personal Access Tokens
2. Създайте нов token с `repo` permissions
3. Копирайте токена
4. В Admin Panel → Editor Mode → GitHub Sync Panel
5. Поставете токена в полето "GitHub Personal Access Token"
6. Натиснете "Запази"

#### Стъпка 2: Преглед на Промените

1. Натиснете "Преглед на Промените"
2. Вижте какъв код ще бъде генериран
3. Прегледайте файловете, които ще бъдат променени

#### Стъпка 3: Приложете Промените

**Опция А: Директен Commit**
1. Въведете commit message (например "Update from Editor Mode")
2. Натиснете "Commit Промените Директно"
3. Промените се прилагат направо в main branch

**Опция Б: Pull Request (Препоръчително)**
1. Въведете PR Title (например "Editor Mode Updates")
2. Опционално добавете описание
3. Натиснете "Създай Pull Request"
4. Създава се нов branch и PR
5. Прегледайте PR в GitHub преди да го merge-нете

**Опция В: Ръчно Прилагане**
1. Натиснете "Свали като Patch"
2. Изтегля се .patch файл
3. Прилагате го ръчно с `git apply` или copy-paste кода

## 🔧 Технически Детайли

### Генериране на Код

Editor Mode генерира следните неща:

1. **JSX Коментари** - Всички TODO-та и инструкции се добавят като коментари в кода
2. **Conditional Rendering Logic** - Генерира се helper функции за условно показване на елементи
3. **TypeScript Exports** - Създава се конфигурация, която може да се импортира
4. **Documentation** - Автоматично се добавя документация в кода

### Пример Генериран Код

```typescript
/**
 * Обща Информация
 * Auto-generated from Editor Mode
 * Last modified: 2025-01-15T10:30:00.000Z
 * 
 * EDITOR MODE CHANGES:
 * - 3 elements are hidden
 * - 2 active comments/TODO items
 */

/**
 * EDITOR MODE: Hidden Elements
 * The following elements are hidden by editor mode configuration.
 */
// HIDDEN: biometric-section
// HIDDEN: lifestyle-metric-stress
// HIDDEN: goal-badge-2

/**
 * EDITOR MODE: Active Comments & TODO Items
 */
// TODO [goals-section] (15.01.2025): Добави икони пред всяка цел
// TODO [system-scores-chart] (15.01.2025): Промени цвета на графиката

// Editor Mode Visibility Configuration
const EDITOR_HIDDEN_ELEMENTS = new Set([
  'biometric-section',
  'lifestyle-metric-stress',
  'goal-badge-2',
])

export const shouldRenderElement = (elementId: string): boolean => {
  return !EDITOR_HIDDEN_ELEMENTS.has(elementId)
}
```

### Използване на Генерирания Код

В React компонента:

```tsx
// Import the config
import { shouldRenderElement } from './OverviewTabEditorConfig'

// Use in JSX
{shouldRenderElement('biometric-section') && (
  <BiometricSection data={data} />
)}

// Or with className
<div className={cn(
  'my-element',
  !shouldRenderElement('element-id') && 'hidden'
)}>
```

## 📊 Storage и Persistance

### Локално Storage

Всички промени се записват в `localStorage`:

```
editor-mode-config           → Главна конфигурация за текущи репорти
editable-elements-config     → Елементи за текущи репорти
editor-config-{reportId}     → Конфигурация за конкретен исторически репорт
editable-elements-{reportId} → Елементи за конкретен исторически репорт
github-token                 → GitHub token (опционално)
```

### GitHub Storage

Когато синхронизирате с GitHub:
- Създават се реални commit-и в repository
- Промените стават част от Git историята
- Могат да се преглеждат чрез `git log`
- Могат да се върнат чрез `git revert`

## 🔐 Сигурност

### GitHub Token

- Токенът се съхранява само в `localStorage` на вашия браузър
- Никога не се изпраща на сървър (освен GitHub API)
- Препоръчваме да използвате token с минимални permissions (`repo` само)
- Можете да го изтриете по всяко време от Admin Panel

### Code Generation

- Генерираният код е статичен TypeScript/React код
- Не съдържа зависимости към runtime конфигурация
- Може да се прегледа и валидира преди commit
- Всички промени са traceable в Git история

## 🎨 Best Practices

### За Коментари

✅ **Добре:**
```
"Добави икона пред всяка цел в goals-section"
"Промени цвета на 'concern' зони от червено на оранжево"
"Направи този контейнер collapsible по подразбиране"
```

❌ **Лошо:**
```
"Промени го"
"Не ми харесва"
"Оправи"
```

### За Git Commits

✅ **Добре:**
```
"feat(editor): Hide stress metric and update goals section"
"fix(editor): Apply editor mode changes to biometric data"
"refactor(editor): Update visibility config for overview tab"
```

❌ **Лошо:**
```
"Update"
"Changes"
"Fixed"
```

### За Pull Requests

1. Винаги прегледайте PR-а в GitHub преди merge
2. Тествайте промените локално след merge
3. Добавяйте screenshots на промените (ако са визуални)
4. Линквайте към related issues (ако има)

## 🐛 Troubleshooting

### GitHub Token не работи

1. Проверете дали token-а има `repo` permissions
2. Проверете дали не е expired
3. Проверете дали repository-то е достъпно за token-a
4. Опитайте да изтриете и добавите token-a отново

### Генерираният код не се прилага

1. Проверете дали файловете съществуват в repository
2. Проверете дали branch-а е правилен
3. Проверете за merge conflicts
4. Опитайте с Pull Request вместо директен commit

### Промените не се показват в репорта

1. Проверете дали Editor Mode е включен
2. Проверете дали елементът е скрит или видим
3. Refresh-нете страницата
4. Проверете console за грешки

## 📚 Допълнителни Ресурси

- `EDITOR_MODE_DOCS.md` - Детайлна документация за базовия Editor Mode
- `EDITOR_SYSTEM_README.md` - Пълна система за редактиране
- `CARD_EDITOR_GUIDE.md` - Ръководство за редактиране на карти

## 🚀 Бъдещи Подобрения

Планирани функции:

- [ ] AI-базирано автоматично прилагане на коментарите
- [ ] Visual diff в интерфейса
- [ ] Undo/Redo за промените
- [ ] Templates за често използвани конфигурации
- [ ] Collaborative editing (множество потребители)
- [ ] Version control с timeline view
- [ ] Export/Import на конфигурации между projects
- [ ] Real-time preview на промените

## 💡 Примери за Използване

### Пример 1: Скриване на Биометрични Данни

1. Включете Editor Mode
2. Намерете "biometric-section" елемента
3. Кликнете иконата "око" → елементът се скрива
4. Отидете в GitHub Sync Panel
5. Създайте PR с название "Hide biometric data from overview"
6. Merge-нете PR-а
7. Промяната е вече перманентна в кода

### Пример 2: Добавяне на Инструкции за AI

1. Намерете елемента, който искате да се промени
2. Добавете коментар: "Промени цвета на графиката от син на зелен"
3. Коментарът се записва в localStorage
4. При GitHub sync, коментарът става TODO в кода
5. AI агентът чете TODO-то и прави промяната
6. След промяната, resolve-вате коментара

### Пример 3: Редактиране на Стар Репорт

1. Отидете в History
2. Изберете репорт от преди 1 месец
3. Натиснете "Редактирай"
4. Скрийте някои секции, които вече не са актуални
5. Добавете коментари за подобрения
6. Запазете промените
7. Репортът сега ще се показва с новата конфигурация

## ❓ FAQ

**В: Губят ли се промените при refresh?**
О: Не, всички промени са в localStorage и се запазват.

**В: Могат ли други хора да видят моите промени?**
О: Не, докато не ги sync-нете с GitHub. След sync всички имат достъп.

**В: Какво се случва с старите репорти след промяна в кода?**
О: Старите репорти имат своя собствена конфигурация и не се засягат от промените в кода.

**В: Мога ли да връщам промени?**
О: Да, чрез Git можете да направите `git revert` на commit-а.

**В: Безопасно ли е да давам GitHub token?**
О: Token-ът се пази само в браузъра ви. Никой друг няма достъп до него.

**В: Може ли да sync-вам с private repository?**
О: Да, стига token-ът да има permissions за този repo.
