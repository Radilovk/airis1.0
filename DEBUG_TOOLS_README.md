# 🛠️ Инструменти за Диагностика и Отстраняване на Проблеми

## Преглед

Приложението сега разполага с мощна система за диагностика и отстраняване на проблеми, която помага за бързо идентифициране и решаване на технически проблеми.

---

## 🚀 Нови Функционалности

### 1. **Startup Checks** (Стартова Проверка)
Автоматично се изпълнява при всяко зареждане на приложението.

**Какво прави:**
- Проверява налични API-та (Spark, KV, LLM, User)
- Проверява браузърни възможности (localStorage, fetch, Promise)
- Логва резултатите в конзолата

**Как да го видите:**
1. Натиснете `F12` → отваря се Developer Console
2. Refresh страницата (`Ctrl+R`)
3. Вижте цветни съобщения в началото на логовете:
   - ✓ Зелено = Всичко е наред
   - ✗ Червено = Има проблеми

**Локация в кода:** `src/lib/startup-checks.ts`

---

### 2. **QuickDebugPanel** (Бърз Панел за Проверка) 🆕
Винаги достъпен floating бутон за бърза диагностика.

**Къде е:**
- Жълт кръгъл бутон с икона на бъг (🐛) в долния десен ъгъл
- Винаги видим на всички екрани
- Кликнете го за отваряне

**Какво показва:**
- Общ статус (Здрава / С проблеми / Критично)
- Брой успешни проверки (🟢)
- Брой предупреждения (🟡)
- Брой грешки (🔴)
- Детайлна информация за всяка проверка (когато е разширен)

**Функции:**
- Expand/Collapse (↑↓) - Показва детайли
- Опресни - Стартира нова проверка
- Затвори (X) - Скрива панела

**Локация в кода:** `src/components/QuickDebugPanel.tsx`

---

### 3. **Diagnostic Screen** (Пълен Диагностичен Екран) 🆕
Пълноценен екран за задълбочена диагностика.

**Как да го достъпите:**
1. Трябва да сте собственик на приложението
2. Главен екран → бутон "Диагностика" (жълт цвят)
3. Или: Welcome Screen → долу в менюто

**Функционалности:**

#### Проверки:
- ✅ Spark API
- ✅ KV Storage (с тестване на запис/четене)
- ✅ User API
- ✅ LocalStorage
- ✅ Browser APIs
- ✅ Dependencies

#### Действия:
- **Изпълни** - Стартира нова диагностика
- **Експорт** - Експортира резултатите като JSON файл
- **Изтрий Всички Данни** - Изтрива всички KV и localStorage данни (ВНИМАНИЕ!)

**Локация в кода:** `src/components/screens/DiagnosticScreen.tsx`

---

### 4. **Diagnostic Library** (Библиотека за Диагностика)
Помощни функции за програмна диагностика.

**Налични функции:**

```typescript
// Изпълнява всички диагностични проверки
await runDiagnostics(): Promise<DiagnosticResult>

// Експортира резултатите като JSON string
await exportDiagnostics(): Promise<string>

// Изтрива всички данни (KV + localStorage)
await clearAllData(): Promise<void>
```

**Локация в кода:** `src/lib/diagnostics.ts`

---

## 📖 Типови Сценарии за Употреба

### Сценарий 1: "Приложението не работи"

1. **Отворете QuickDebugPanel** (жълт бутон долу вдясно)
2. Проверете статуса:
   - 🟢 Всички зелени? → Проблемът не е в системата
   - 🔴 Има червени? → Кликнете за детайли
3. Ако има червени:
   - Прочетете съобщението
   - Вижте решението в TROUBLESHOOTING_GUIDE_BG.md

### Сценарий 2: "Трябва да докладвам проблем"

1. Отидете в **Diagnostic Screen** (главно меню → Диагностика)
2. Изчакайте да завърши автоматичната проверка
3. Кликнете **"Експорт"**
4. Запазете JSON файла
5. Изпратете файла заедно с описанието на проблема

### Сценарий 3: "Данните са повредени"

1. Отидете в **Diagnostic Screen**
2. Scroll до секция **"Опасна Зона"**
3. Кликнете **"Изтрий Всички Данни"**
4. Потвърдете
5. Приложението ще се презареди
6. Започнете отначало

### Сценарий 4: "Проверка преди важен анализ"

1. Кликнете **QuickDebugPanel** (жълт бутон)
2. Проверете дали всички проверки са зелени
3. Ако да → Продължете с анализа
4. Ако не → Отстранете проблемите първо

---

## 🔧 За Разработчици

### Добавяне на Нова Проверка

**В `src/lib/diagnostics.ts`:**

```typescript
async function checkMyNewFeature(): Promise<DiagnosticCheck> {
  try {
    // Вашата логика за проверка
    const isWorking = true // замете с реална проверка
    
    if (isWorking) {
      return {
        name: 'My Feature',
        status: 'pass',
        message: 'Функцията работи'
      }
    }
    
    return {
      name: 'My Feature',
      status: 'fail',
      message: 'Функцията не работи',
      details: 'Причината за грешката'
    }
  } catch (error) {
    return {
      name: 'My Feature',
      status: 'fail',
      message: 'Грешка при проверка',
      details: error instanceof Error ? error.message : String(error)
    }
  }
}

// Добавете в runDiagnostics():
export async function runDiagnostics(): Promise<DiagnosticResult> {
  const checks: DiagnosticCheck[] = []
  
  // ... съществуващи проверки
  checks.push(await checkMyNewFeature()) // Добавете тук
  
  // ... остатъка от функцията
}
```

### Използване на Диагностиката Програмно

```typescript
import { runDiagnostics, clearAllData } from '@/lib/diagnostics'

// В компонент или функция
const handleCheckSystem = async () => {
  const result = await runDiagnostics()
  
  if (result.overallStatus === 'critical') {
    console.error('Критични проблеми!', result.checks)
    // Вземете действие
  }
}

// Изтриване на данни
const handleReset = async () => {
  if (confirm('Сигурни ли сте?')) {
    await clearAllData()
    window.location.reload()
  }
}
```

### Добавяне на Стартова Проверка

**В `src/lib/startup-checks.ts`:**

```typescript
// Добавете нова проверка в performStartupChecks()
const hasMyAPI = typeof window.myAPI !== 'undefined'
checks.push({
  name: 'My API',
  passed: hasMyAPI,
  error: hasMyAPI ? undefined : 'myAPI is not available'
})

if (!hasMyAPI) {
  criticalErrors.push('My API липсва - функцията X няма да работи')
}
```

---

## 📂 Файлова Структура

```
src/
├── lib/
│   ├── diagnostics.ts          # Основна библиотека за диагностика
│   └── startup-checks.ts       # Стартови проверки
├── components/
│   ├── QuickDebugPanel.tsx     # Floating панел за бърза проверка
│   └── screens/
│       └── DiagnosticScreen.tsx # Пълен диагностичен екран
└── main.tsx                     # Стартира autoRunStartupChecks()
```

---

## 📚 Документация

- **TROUBLESHOOTING_GUIDE_BG.md** - Пълно ръководство за отстраняване на проблеми
- **QUICK_REFERENCE_BG.md** - Бърза справка за структурата на проекта
- **DEBUG_TOOLS_README.md** - Този файл

---

## ✅ Checklist за Проверка

Използвайте този checklist, когато нещо не работи:

- [ ] Отворил съм QuickDebugPanel и видях статуса
- [ ] Всички проверки в QuickDebugPanel са зелени
- [ ] Проверил съм конзолата (F12) за грешки
- [ ] Презаредил съм страницата (Ctrl+R)
- [ ] Изчистил съм кеша ако е необходимо
- [ ] Експортирал съм диагностиката ако ще докладвам проблем
- [ ] Прочел съм TROUBLESHOOTING_GUIDE_BG.md

---

## 🎯 Често Задавани Въпроси

### Как да скрия QuickDebugPanel?
Просто кликнете X-а в горния десен ъгъл на панела. За да го покажете отново, кликнете жълтия бутон.

### Мога ли да използвам диагностиката в production?
Да! QuickDebugPanel и Diagnostic Screen са безопасни за production. Пълният Diagnostic Screen е достъпен само за собственици.

### Какви данни се експортират?
Само резултатите от проверките - име, статус, съобщение и детайли. Никакви лични данни, снимки или медицинска информация.

### Какво прави "Изтрий Всички Данни"?
Изтрива всички KV Storage записи и localStorage данни. След това презарежда страницата. Действието е необратимо!

### Може ли да добавя свои проверки?
Да! Вижте секцията "За Разработчици" по-горе.

---

## 🚨 Важни Забележки

1. **QuickDebugPanel винаги е достъпен** - Дори ако главното приложение не работи, той ще се опита да се зареди
2. **Startup checks логват в конзолата** - Винаги проверявайте конзолата при стартиране
3. **Изтриване на данни е необратимо** - Винаги експортирайте докладите преди да изтриете
4. **Diagnostic Screen е само за собственици** - Обикновените потребители не го виждат

---

## 📞 За Помощ

Ако имате проблем който не можете да решите:

1. Експортирайте диагностиката
2. Направете screenshot на грешката
3. Копирайте грешките от конзолата (F12)
4. Опишете какво правихте когато възникна проблемът
5. Изпратете всичко на разработчика

---

**Последна актуализация:** v21 - Декември 2024

**Създадено от:** Spark Agent - Iteration 21
