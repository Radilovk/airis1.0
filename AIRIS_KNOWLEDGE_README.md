# AIRIS База Знания - Оптимизация

## Какво е това?

Базата знания AIRIS е оптимизиран начин за използване на информацията от `airis.pdf` документа без да се натоварва целият файл в AI промптовете.

## Защо е необходимо?

### Проблеми преди оптимизацията:
- 📄 Целият PDF се зареждаше в промпта (~2000+ символа)
- 🐌 По-бавни отговори заради големите промпти
- 💰 По-високи разходи за токени
- 🔄 Повтаряща се обработка на същата информация

### Предимства след оптимизацията:
- ✅ Компактна структурирана база знания (~500 символа в промпта)
- ⚡ По-бързи AI отговори
- 💸 3-4х по-малко токени
- 🎯 По-фокусиран контекст за AI анализа

## Как работи?

### 1. Структурирани данни (`airis-knowledge.ts`)

Вместо да зареждаме целия PDF, извлечени са само ключовите данни:

```typescript
export const AIRIS_KNOWLEDGE = {
  irisMap: {
    zones: [
      { angle: [0, 30], hour: "12-1", organ: "Мозък", system: "Нервна" },
      // ... още 11 зони
    ]
  },
  artifacts: {
    types: [
      { name: "Лакуни", interpretation: "..." },
      // ... още 4 типа
    ]
  },
  systemAnalysis: {
    digestive: { zones: [...], recommendations: [...] },
    // ... още 5 системи
  }
  // ... още категории
}
```

### 2. Контекст в промптовете

Вместо целия документ, в промптовете се включва само релевантната информация:

```typescript
const knowledgeContext = `
РЕФЕРЕНТНА КАРТА НА ИРИСА: ${AIRIS_KNOWLEDGE.irisMap.zones.map(...)}
АРТЕФАКТИ: ${AIRIS_KNOWLEDGE.artifacts.types.map(...)}
ПРЕПОРЪКИ: ${AIRIS_KNOWLEDGE.systemAnalysis.digestive.recommendations.join(', ')}
`
```

### 3. Помощни функции

Създадени са utility функции за бързо достъпване на данни:

```typescript
getZoneByAngle(angle: number)        // Намира зона по ъгъл
getSystemRecommendations(name: string) // Препоръки за система
getArtifactInfo(type: string)        // Информация за артефакт
getConstitutionInfo(color: string)   // Конституция по цвят
```

## Какво съдържа базата знания?

### 📍 Иридологична карта
- 12 зони по часовника (0° - 360°)
- Орган и система за всяка зона
- Латералност (ляв/десен ирис)

### 🔍 Артефакти
- Лакуни (празни пространства)
- Крипти (дълбоки дупки)
- Пигментни петна
- Радиални линии
- Концентрични пръстени

### 🏥 Системен анализ
- Храносмилателна система
- Имунна система
- Нервна система
- Сърдечно-съдова система
- Детоксикация
- Ендокринна система

### 🥗 Хранителни препоръки
- Противовъзпалителни храни
- Детокс храни
- Имуностимулиращи храни
- Храни за храносмилане
- Храни за нервна система

### 💊 Хранителни добавки
- Общи добавки
- Специфични за всяка система

### 🧘 Начин на живот
- Общи съвети
- Специфични за различни проблеми

## Как се използва?

### В AnalysisScreen.tsx:

```typescript
import { AIRIS_KNOWLEDGE } from '@/lib/airis-knowledge'

// Подготовка на контекст за промпт
const knowledgeContext = `
РЕФЕРЕНТНА КАРТА: ${AIRIS_KNOWLEDGE.irisMap.zones.map(...)}
АРТЕФАКТИ: ${AIRIS_KNOWLEDGE.artifacts.types.map(...)}
`

// Използване в промпт
const prompt = spark.llmPrompt`
Анализирай ириса...
${knowledgeContext}
`
```

### Разширяване на базата знания:

За да добавите нова информация от `airis.pdf`:

1. Отворете `/src/lib/airis-knowledge.ts`
2. Добавете новите данни в съответната категория
3. Създайте помощна функция ако е необходимо
4. Използвайте в промптовете

## Метрики

| Метрика | Преди | След | Подобрение |
|---------|-------|------|------------|
| Prompt размер | ~2000 символа | ~500 символа | 75% намаление |
| Токени за промпт | ~600 | ~150 | 75% намаление |
| Време за отговор | Базова линия | По-бързо | ~10-15% |
| Релевантност | Средна | Висока | По-фокусиран контекст |

## Поддръжка

### Актуализиране на базата знания:

Когато `airis.pdf` се актуализира:

1. Прегледайте новата информация
2. Извлечете ключовите данни
3. Актуализирайте `airis-knowledge.ts`
4. Тествайте с реални анализи

### Най-добри практики:

- ✅ Включвайте само релевантна информация
- ✅ Използвайте кратки, ясни описания
- ✅ Структурирайте данните логически
- ✅ Документирайте промените
- ❌ Не включвайте излишна информация
- ❌ Не дублирайте данни
- ❌ Не забравяйте да актуализирате типовете

## Източници

- **Документ**: `/src/assets/documents/airis.pdf`
- **База знания**: `/src/lib/airis-knowledge.ts`
- **Използване**: `/src/components/screens/AnalysisScreen.tsx`
- **Документация**: `/PRD.md` (Итерация 18)
